# tmux cli

Session
`tmux ls`: List sessions
`tmux new-session -s <session_name>`: create new session 
`tmux attach` | `tmux a`: Attach to last session: 

`tmux attach -t <session-name>`
`tmux a -t <session-name>`


