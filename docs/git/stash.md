# Git stash

This temporarily saves your uncommitted changes and cleans your working directory.

`git stash`

Apply stash
`git stash pop` 

Get stash lish
`git stash list`

`git stash apply stash@{1}`

Clear all
`git stash clear`
